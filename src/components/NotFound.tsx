import React, { FC } from "react";
import { Typography } from "@mui/material";

export const NotFound: FC = () => <Typography
  variant="h1"
  component="h1"
  sx={{ textAlign: 'center' }}>
    404
</Typography>;
